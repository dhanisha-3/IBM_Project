export interface CodeExplanation {
  code: string;
  language: string;
  explanation: string;
  timestamp: number;
}

export interface ExampleCode {
  title: string;
  language: string;
  code: string;
  description: string;
}